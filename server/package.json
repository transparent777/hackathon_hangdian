{
  "name": "cyber-companion-server",
  "version": "0.1.0",
  "private": true,
  "description": "赛博陪伴相机后端 API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
