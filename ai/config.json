{
  "_readme": "非敏感 AI 默认参数。密钥、基址见 server/.env.example。",
  "blend": {
    "defaultStrength": 0.65,
    "timeoutMs": 90000,
    "maxRetries": 1
  },
  "diary": {
    "timeoutMs": 45000,
    "maxRetries": 1
  },
  "providers": {
    "mock": {
      "description": "不回传真实溶图，原图作 resultUrl，日记用 fallback"
    },
    "http": {
      "description": "OpenAI 兼容或自建网关；在 providers/http.js 实现",
      "blendEndpoint": "/v1/images/edits",
      "diaryEndpoint": "/v1/chat/completions"
    }
  }
}
