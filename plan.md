# 赛博陪伴相机 · 分工与计划表

> **截止**：2026-09-12  
> **平台**：微信小程序  
> **Demo IP**：奶蛙 / doro / 耄耋  
> **团队**：4 人  
> **今日**：2026-09-07（剩余 5 天）

---

## 一、产品一句话

每天随机 roll 一只热门虚拟生物，溶进你的真实生活照片，生成「赛博陪伴」瞬间。

**答辩黄金 Demo**：打开小程序 → 今日 roll 到「耄耋」→ 拍书桌 → 10 秒内出图「耄耋趴在键盘旁」+ 一句陪伴语 → 保存分享。

---

## 二、四人分工

| 角色 | 代号 | 主要职责 | 交付物 |
|------|------|----------|--------|
| **小程序前端** | A | 页面、拍照/选图、roll 动画、结果展示、日记列表、分享 | 可运行的小程序 + 与后端联调通过 |
| **后端 & 基建** | B | 云函数/Node 服务、OSS、用户态、roll 逻辑、限流、接口文档 | `/roll` `/blend` `/history` 三个核心 API |
| **AI & Prompt** | C | 溶图 API 对接、三角色 prompt 模板、参考图、效果调优、陪伴语生成 | 10 张标杆样图 + prompt 配置表 |
| **产品 & 设计 & 答辩** | D | UI 稿、角色素材整理、测试用例、答辩 PPT、演示脚本、录屏 | Figma/稿图 + PPT + 3 条演示视频 |

### 协作边界（避免踩脚）

```
A ←→ B：接口联调（每天 18:00 对一次）
C → B：prompt/角色配置以 JSON 交给 B 落库，C 不直接改生产环境
D → A：UI 切图与文案；D → C：角色参考图与「像不像」验收
全员：9/10 起每天晚 21:00 站会 15 分钟，只报阻塞项
```

### 建议技能匹配

| 若同学擅长… | 优先角色 |
|-------------|----------|
| 微信小程序 / uni-app | A |
| Node / Python / 云开发 | B |
| 调过大模型 / 玩过 SD 或图像 API | C |
| 会做 PPT、审美在线、爱写文案 | D |

---

## 三、MVP 功能清单（必须做）

- [ ] 每日 roll 一只陪伴兽（奶蛙 / doro / 耄耋，可伪随机 + 本地种子）
- [ ] 拍照或相册选图
- [ ] 上传 → AI 溶图 → 返回结果图
- [ ] 结果页：图片 + 角色名 + 一句陪伴语
- [ ] 保存到相册 / 转发好友（小程序分享卡片）
- [ ] 简单历史记录（最近 10 条，本地或云端）

### 加分项（时间够再做）

- [ ] roll 稀有度动画（普通 / 稀有闪一下）
- [ ] 陪伴日记时间线 UI
- [ ] 同一角色连续出现 2 天的「羁绊」文案
- [ ] 生成中趣味 loading 文案轮播

### 明确不做（防 scope 爆炸）

- 账号体系 / 登录（用 openid 匿名即可）
- 付费、商城、复杂养成
- 实时 AR 相机预览
- 第四个 IP

---

## 四、技术架构（简版）

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│ 微信小程序   │────▶│ 后端 API      │────▶│ 对象存储 OSS │
│ 拍照/roll/UI │     │ roll/blend   │     │ 原图/结果图  │
└─────────────┘     └──────┬───────┘     └─────────────┘
                           │
                    ┌──────▼───────┐
                    │ AI 图像 API   │
                    │ + LLM 陪伴语  │
                    └──────────────┘
```

**推荐栈**

| 层 | 选型 |
|----|------|
| 小程序 | 微信原生 或 uni-app（团队熟哪个用哪个） |
| 后端 | 微信云开发云函数 **或** 轻量 Node（Express/Fastify）部署云服务器 |
| 存储 | 腾讯云 COS / 阿里云 OSS / 云开发存储 |
| AI 溶图 | 现有账号对应 API（图生图 / 图像编辑，带参考图） |
| 陪伴语 | 同厂商文本模型或轻量 prompt 模板（先模板，不稳再上 LLM） |

---

## 五、核心接口（B 负责，9/8 前定稿）

### `POST /api/roll`

- 入参：`openid`（或设备指纹）
- 出参：`{ characterId, name, rarity, quote }`
- 规则：每日首次固定种子；可缓存到当天 24:00

### `POST /api/blend`

- 入参：`openid`, `characterId`, `image`（base64 或 OSS url）
- 出参：`{ resultUrl, companionText, taskId }`
- 内部：调 C 的 prompt 模板 + AI API，结果上传 OSS

### `GET /api/history?openid=`

- 出参：最近 N 条 `{ thumbUrl, characterName, createdAt, quote }`

---

## 六、AI 侧配置（C 负责）

### 三角色最小素材包（每人 9/8 中午前）

| characterId | 显示名 | 参考图 | prompt 关键词 |
|-------------|--------|--------|----------------|
| `naiwa` | 奶蛙 | 2–3 张正面全身 | chibi milk dragon, cute, round face, milk bottle |
| `doro` | doro | 2–3 张 | orange simple creature, meme style, silly expression |
| `maodie` | 耄耋 | 2–3 张 | fluffy old cat meme, lazy, squished face |

### 溶图 prompt 模板（示例）

```
将参考图中的【{name}】自然融入用户照片的场景中：
- 保持真实环境为主，不要整张图变成动漫风格
- 角色坐在/站在【桌面/地面/沙发】等合理位置
- 添加与环境一致的光照和接触阴影
- 比例符合透视，大小约为【场景参照物】的 1/3
- 风格：可爱、治愈、赛博陪伴感
```

### 验收标准（C 自测 10 张）

- 场景可辨认（不像整张重绘）
- 角色可辨认（路人能说出是哪个梗）
- 无明显「贴纸感」白边（≥7/10 张合格即可 demo）

---

## 七、倒排日程

### 9/07（周日）· 今天 · 对齐日

| 时间 | 全员 | 产出 |
|------|------|------|
| 下午 | 开 30 分钟会：定产品名、分工、技术栈 | 本文档确认版 |
| 晚 | A：注册小程序 + 空项目；B：仓库 + 云环境；C：跑通 1 次 AI 溶图；D：拉 UI 参考 + 三角色参考图 | 各环境 Hello World |

**今日阻塞必须清零**：AI API 能否出一张合格溶图（C）

---

### 9/08（周一）· 通路日

| 角色 | 任务 |
|------|------|
| **C** | 三角色各 3 张标杆图；prompt v1；陪伴语模板 9 条 |
| **B** | `/blend` 端到端：收图 → 调 AI → 存 OSS → 返 url |
| **A** | 首页 + 拍照页 + 上传逻辑（可先 mock 返回） |
| **D** | 首页/结果页 UI 稿；写 5 条测试场景（室内桌/户外/沙发/夜景/食物旁） |

**里程碑**：Postman/curl 调 `/blend` 能返回一张图。

---

### 9/09（周二）· 联调日

| 角色 | 任务 |
|------|------|
| **A** | roll 页动画；接真实 `/blend`；loading / 错误态 |
| **B** | `/roll` + `/history`；openid；每日限次（如 20 次/日） |
| **C** | 根据 A 测试反馈调 prompt；失败重试策略 |
| **D** | 全流程走查；记 bug 列表；开始 PPT 大纲 |

**里程碑**：小程序真机从头到尾跑通 1 次。

---

### 9/10（周三）· 体验日

| 角色 | 任务 |
|------|------|
| **A** | 结果页分享、保存相册、历史列表；UI 细节 |
| **B** | 超时处理、图片压缩、接口日志 |
| **C** | 三角色效果终调；可选 LLM 陪伴语 |
| **D** | 答辩 PPT v1；准备 3 张「惊艳」样图；写 30 秒开场白 |

**里程碑**：外人试用 3 人，收集反馈并修 top3 bug。

---

### 9/11（周四）· 冻结日

| 角色 | 任务 |
|------|------|
| **A** | 仅修 bug，不加新功能 |
| **B** | 部署稳定环境；备份 API Key |
| **C** | 固定 prompt 版本号，不再大改 |
| **D** | PPT 终版；演示脚本排练 3 遍；录屏备用 |

**规则**：18:00 功能冻结，之后只修崩溃级问题。

---

### 9/12（周五）· 提交日

| 时段 | 事项 |
|------|------|
| 上午 | 最终冒烟测试；提交 hackathon 材料 |
| 下午 | 答辩彩排；现场 demo 机充电、清缓存 |
| 晚上 | 答辩 / 展示 |

---

## 八、风险与预案

| 风险 | 预案 |
|------|------|
| AI 溶图太慢（>30s） | 异步：先返回 taskId，前端轮询；或降低分辨率 |
| 角色不像 / 崩图 | 降级为「贴纸合成」：C 出透明 PNG + A 前端叠图（备用方案，9/9 前决定是否启用） |
| 小程序审核来不及 | 体验版二维码演示 + 录屏；赛前先提审 |
| API 超预算 | 每日每用户限 5–10 次；测试阶段用小图 |
| 某人卡住 | 每日站会报红；B 和 C 互为 backup 调 API |

---

## 九、答辩叙事（D 写入 PPT）

1. **痛点**：孤独感、想记录生活但缺「陪伴感」  
2. **灵感**：万物奶蛙 + 梗文化 + 赛博陪伴  
3. **创新**：Gacha roll + 溶图融入（非整图替换）+ 陪伴叙事  
4. **技术**：小程序 + 多模态 AI 溶图 + 角色 prompt 工程  
5. **Demo**：现场 roll → 拍 → 出图  
6. **未来**：更多 IP、日记、轻养成、AR

---

## 十、仓库目录建议（B 9/8 初始化）

```
hackathon_hangdian/
├── miniprogram/          # A：小程序前端
├── server/               # B：后端 API
├── ai/
│   ├── prompts/          # C：三角色 prompt JSON
│   └── references/       # C：参考图
├── docs/
│   ├── PLAN.md           # 本文件
│   └── api.md            # 接口文档
└── pitch/                # D：PPT、样图、录屏
```

---

## 十一、每日站会模板（21:00，15 分钟）

1. 昨天完成了什么（每人 1 句）  
2. 今天计划（每人 1 句）  
3. 阻塞项（只讨论阻塞）  
4. 明天里程碑是否仍可达（是/否）

---

*最后更新：2026-09-07*
